---
label: blog
hide_body: true
display_field: title
fields:
- name: template
  type: text
  config:
    required: false
  label: template
  hidden: true
  default: blog.twig
- name: list_of_blogposts
  type: list
  config:
    use_select: true
    min: 
    max: 
    source:
      type: pages
      section: blog
  label: List of blogposts
- name: title
  type: text
  config:
    required: false
  label: Title

---
label: blog
hide_body: true
display_field: title
fields:
- name: template
  type: text
  config:
    required: false
  label: template
  hidden: true
  default: blog.twig
- name: test
  type: select
  default: []
  config:
    required: false
    options: []
    source:
      type: pages
      section: pages
      file: 
      path: 
  label: Test
- name: test_blog_select
  type: list
  config:
    use_select: true
    min: 
    max: 
    source:
      type: pages
      section: blog
  label: Test blog select
